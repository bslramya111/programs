int main()
{
int n,rem,num,reverse=0;
    printf("enter a three digit no");
    scanf("%d",&n);
    num=n;
    while(n>0)
    {
      rem=n%10;
      reverse=reverse*10+rem;
      n=n/10;
    }
    printf("%d",reverse);
        return 0;
}

 
